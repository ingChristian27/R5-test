import React from "react";

import { Box, Typography } from "@mui/material";

const Bookstore = () => {
  return (
    <Box>
      <Typography> This is a demo page Bookstore</Typography>
    </Box>
  );
};

export default Bookstore;
